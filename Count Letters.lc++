#include <iostream>
#include <cctype>
using namespace std;

int main() {

    string s;
    getline(cin, s);

    int count = 0;

    for(char c : s)
        if(isalpha(c))
            count++;

    cout << count;
}
